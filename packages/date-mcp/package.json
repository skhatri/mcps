{
  "name": "@skhatri/date-mcp",
  "version": "0.8.0",
  "description": "MCP (Model Context Protocol) server for date and time operations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "date-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "publish": "npm publish"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "date",
    "time",
    "datetime"
  ],
  "author": "skhatri",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/skhatri/mcps.git",
    "directory": "packages/date-mcp"
  },
  "publishConfig": {
    "access": "public"
  }
}
