{
  "name": "@skhatri/git-mcp",
  "version": "0.6.0",
  "description": "MCP (Model Context Protocol) server for Git operations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "git-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "publish": "npm publish"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "git",
    "version-control",
    "scm"
  ],
  "author": "skhatri",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/skhatri/mcps.git",
    "directory": "packages/git-mcp"
  },
  "publishConfig": {
    "access": "public"
  }
}
